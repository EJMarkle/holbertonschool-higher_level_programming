Class that inherits from list:

Function that prints sorted list
'print_sorted()'

MyList = __import__('1-my_list').MyList

Negative Integers:

   >>> my_list_negative = MyList([-4, -2, -6, -1, -8, -3])

Large List:

    >>>large_list = MyList(range(10**6, 0, -1))

Mixed Data Types:

    >>> my_list_mixed = MyList([4, 2, 'abc', 1, 8, 3])

Duplicate Elements:

    >>> my_list_duplicates = MyList([4, 2, 2, 1, 4, 3])

Single Elements:

    >>> my_list_empty = MyList([])
    >>> my_list_single = Mylist([42])
